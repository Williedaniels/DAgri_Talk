# D'Agri Talk Environment Variables for Docker Compose
# Copy this file to .env for local development.

# --- Backend Configuration ---

# Flask environment: development or production
FLASK_ENV=development

# Secret keys for session management and security.
# For real development, generate these with `openssl rand -hex 32` in your terminal.
SECRET_KEY=a-very-secret-key-for-development
JWT_SECRET_KEY=another-super-secret-key-for-jwt

# Database connection URL for the backend service to connect to the database service.
# The hostname 'database' is the service name in docker-compose.yml.
DATABASE_URL=postgresql://dagri_user:dagri_password@database:5432/dagri_talk_dev

# Redis connection URL. The hostname 'redis' is the service name in docker-compose.yml.
REDIS_URL=redis://redis:6379/0

# --- Frontend Configuration ---

# The URL for the frontend (running in the user's browser) to reach the backend API.
REACT_APP_API_URL=http://localhost:5001/api