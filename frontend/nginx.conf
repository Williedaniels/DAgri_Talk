server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # This line is crucial for single-page applications like React.
    # It directs all requests to index.html, allowing React Router to handle them.
    try_files $uri $uri/ /index.html;
  }
}